@model IEnumerable<FormClean.Application.DTOs.ClientDTO>

@{
    ViewData["Title"] = "Clientes";
}

<div>
    <h2>@ViewData["Title"]</h2>
</div>
<hr />
<div class="row">
    <div class="col-md-12">
        <div>
            <div class="pull-left">
                <a asp-action="Create" class="btn btn-primary">
                    <span title="Cadastrar Cliente" class="fas fa-plus"></span> Cadastrar Cliente
                </a>
            </div>
        </div>
    </div>
</div>
<br />
<div class="panel panel-default">
    <table id="table" class="table table-striped cell-border">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Name)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Phone)
                </th>
                <th></th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.Name)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Phone)
                    </td>
                    <td></td>
                    <td>
                        <div class="row">
                            <div class="m-2">
                                <a asp-action="Details" asp-route-id="@item.Id" title="Details" class="btn btn-primary">
                                    <span class="fas fa-search">Detalhes</span>
                                </a>
                            </div>
                            <div class="m-2">
                                <a asp-action="Delete" asp-route-id="@item.Id" title="Delete" class="btn btn-danger">
                                    <span class="fas fa-trash-alt">Deletar</span>
                                </a>
                            </div>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
